import styled from "styled-components";

const Clima = styled.section`
  background: var(--branco);
  box-shadow: var(--sombra);
  border-radius: 20px;
  padding: 24px;
  margin: 18px;
`

export default Clima;